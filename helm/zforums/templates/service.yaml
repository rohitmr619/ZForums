apiVersion: v1
kind: Service
metadata:
  name: {{ include "zforums.fullname" . }}
  labels:
    {{- include "zforums.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zforums.selectorLabels" . | nindent 4 }}
